Скрипт для генерации коллекции Anki.

# Настройка окружения

Необходимо, чтобы в системе был Python 3.8+

```sh
python -m venv venv
source venv/bin/activate.bash
pip install -r requirements.txt
```

# Использование

```sh
./questions/generate.py
```
